---
import Layout from '../../../../layouts/BlogIndex.astro';
import { getStaticTagPaths } from '../../../../tools/blog';
import { startCase } from '../../../../tools/string';

export async function getStaticPaths({paginate}) {
  return getStaticTagPaths(paginate);
}

const {page, tagName} = Astro.props;
const {tag} = Astro.params;
---
<Layout title={startCase(tagName) + " posts"}
        section="tag"
        sectionTitle="Tags"
        subjectRef={tag}
        subjectName={tagName}
        page={page}
        showCategories={true}/>
